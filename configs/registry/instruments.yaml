version: "1.0"
instruments:
  - id: "CME.MNQ"
    display_name: "E-mini Nasdaq-100"
    type: "future"
    default_profile: "CME_MNQ_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CME"
    multiplier: 2.0
    tick_size: 0.25
    tick_value: 0.50
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "TWF.MXF"
    display_name: "Taiwan Futures Exchange Mini-TAIEX"
    type: "future"
    default_profile: "TWF_MXF_TPE_v1"
    currency: "TWD"
    cost_model:
      # Commission in instrument currency (TWD)
      commission_per_side: 50
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "TAIFEX"
    multiplier: 50.0
    tick_size: 1.0
    tick_value: 50.0
    timezone: "Asia/Taipei"
    trade_date_roll_time_local: "15:00"

  - id: "CFE.VX"
    display_name: "VIX Futures"
    type: "future"
    default_profile: "CFE_VX_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CFE"
    multiplier: 1000.0
    tick_size: 0.05
    tick_value: 50.0
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "CME.6J"
    display_name: "Japanese Yen Futures (6J)"
    type: "future"
    default_profile: "CME_6J_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CME"
    multiplier: 12500000.0
    tick_size: 0.0000005
    tick_value: 6.25
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "CME.CL"
    display_name: "Crude Oil Futures (CL)"
    type: "future"
    default_profile: "CME_CL_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CME"
    multiplier: 1000.0
    tick_size: 0.01
    tick_value: 10.0
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "NYBOT.DX"
    display_name: "U.S. Dollar Index Futures (DX)"
    type: "future"
    default_profile: "NYBOT_DX_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "NYBOT"
    multiplier: 1000.0
    tick_size: 0.005
    tick_value: 5.0
    # NOTE(V1): treat as US exchange hours; roll is defined in local exchange time.
    timezone: "America/New_York"
    trade_date_roll_time_local: "18:00"

  - id: "CBOT.ZN"
    display_name: "10-Year U.S. Treasury Note Futures (ZN)"
    type: "future"
    default_profile: "CBOT_ZN_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CBOT"
    multiplier: 1000.0
    tick_size: 0.015625
    tick_value: 15.625
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "CME.MGC"
    display_name: "Micro Gold Futures (MGC)"
    type: "future"
    default_profile: "CME_MGC_TPE_v1"
    currency: "USD"
    cost_model:
      # Commission in instrument currency (USD)
      commission_per_side: 1.4
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "CME"
    multiplier: 10.0
    tick_size: 0.1
    tick_value: 1.0
    timezone: "America/Chicago"
    trade_date_roll_time_local: "17:00"

  - id: "OSE.NK225M"
    display_name: "Nikkei 225 Mini Futures (NK225M)"
    type: "future"
    default_profile: "OSE_NK225M_TPE_v1"
    currency: "JPY"
    cost_model:
      # Commission in instrument currency (JPY)
      commission_per_side: 250
      # Slippage is fixed at 1 tick per side in V1
      slippage_per_side_ticks: 1
    default_timeframe: 60
    exchange: "OSE"
    multiplier: 100.0
    tick_size: 5.0
    tick_value: 500.0
    timezone: "Asia/Tokyo"
    trade_date_roll_time_local: "15:00"
default: "CME.MNQ"
