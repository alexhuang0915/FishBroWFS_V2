SSOT:
- Source: src/strategy/registry.py (get_strategy_registry, get)
- Schema: src/strategy/param_schema.py (ParamSpec)
- Hardcoded: src/pipeline/runner_adapter.py lines 231, 323

P1 Changes:
- runner_adapter.py: extract strategy_id from cfg, use registry to get schema.
- funnel_runner.py: ensure strategy_id passed in stage_cfg.

UI Observability:
- Error: EvidenceLookupError in src/gui/desktop/services/evidence_locator.py
- Structure: src/gui/services/ranking_explain_builder.py uses Winner/Plateau artifacts.
- Gap: Warnings from runtime (Numba, Env, Plateau Fallback) need to flow to artifacts.
- Plan:
    1. runner_adapter/stage_runners -> returns {..., "warnings": []}
    2. funnel_runner -> collects warnings -> write_run_artifacts
    3. write_run_artifacts -> ranking_explain_builder -> ReasonCards
    4. EvidenceLookupError -> Separate UI Banner handling or Error Code in ReasonCard if caught early.

Specific Warnings:
- WARN_NUMBA_MISSING: Check src/engine/engine_jit.py (if pure python fallback used)
- WARN_PLATEAU_FALLBACK: Check runner_adapter.py (generation of plateau_candidates)
